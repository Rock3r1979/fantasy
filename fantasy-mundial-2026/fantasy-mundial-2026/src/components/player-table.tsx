import { allTeams } from "@/lib/data";
import { formatMoney, formatPct } from "@/lib/utils";
import type { Player } from "@/types";
export function PlayerTable({ players }: { players: Player[] }) { return <div className="table-wrap"><table className="min-w-full text-left text-sm"><thead className="border-b border-border bg-panel text-muted"><tr><th className="px-4 py-3">Jugador</th><th className="px-4 py-3">Pos</th><th className="px-4 py-3">Selección</th><th className="px-4 py-3">Titularidad</th><th className="px-4 py-3">Gol</th><th className="px-4 py-3">Asistencia</th><th className="px-4 py-3">Avance</th><th className="px-4 py-3">Valor</th><th className="px-4 py-3">Score</th></tr></thead><tbody>{players.map((player) => { const team = allTeams.find((item) => item.id === player.teamId); return <tr key={player.id} className="border-b border-border/70 last:border-0 hover:bg-panel/70"><td className="px-4 py-3 font-medium">{player.name}</td><td className="px-4 py-3">{player.position}</td><td className="px-4 py-3">{team?.flag} {team?.shortName}</td><td className="px-4 py-3">{formatPct(player.starterProbability)}</td><td className="px-4 py-3">{formatPct(player.goalProbability)}</td><td className="px-4 py-3">{formatPct(player.assistProbability)}</td><td className="px-4 py-3">{formatPct(player.advanceProbability)}</td><td className="px-4 py-3">{formatMoney(player.fantasyValue)}</td><td className="px-4 py-3 font-semibold">{player.fantasyScore}</td></tr>; })}</tbody></table></div>; }
