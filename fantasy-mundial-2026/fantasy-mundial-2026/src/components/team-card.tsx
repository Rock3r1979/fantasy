import { getPlayersByTeam } from "@/lib/data";
import { formatPct } from "@/lib/utils";
import type { Team } from "@/types";
export function TeamCard({ team }: { team: Team }) { const players = getPlayersByTeam(team.id).sort((a, b) => b.fantasyScore - a.fantasyScore); const starters = players.filter((player) => player.isStarter).slice(0, 4); const bench = players.filter((player) => !player.isStarter).slice(0, 2); return <article className="card p-6"><div className="flex items-start justify-between gap-4"><div><p className="text-3xl">{team.flag}</p><h3 className="mt-3 text-2xl font-bold">{team.name}</h3><p className="text-sm text-muted">Grupo {team.groupId} · Ranking FIFA #{team.fifaRanking}</p></div><span className="badge-soft">Clasificación {formatPct(team.qualifyProbability)}</span></div><div className="mt-6 grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-panel p-4"><p className="text-sm font-semibold">Once probable</p><ul className="mt-3 space-y-2 text-sm text-muted">{starters.map((player) => <li key={player.id}>{player.name} · {player.position}</li>)}</ul></div><div className="rounded-xl bg-panel p-4"><p className="text-sm font-semibold">Suplentes clave</p><ul className="mt-3 space-y-2 text-sm text-muted">{bench.map((player) => <li key={player.id}>{player.name} · {player.position}</li>)}</ul></div></div></article>; }
